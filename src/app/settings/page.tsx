import { PageStub } from "@/components/common/PageStub";

export default function SettingsPage() {
  return (
    <PageStub
      title="설정"
      phase="Phase 6"
      description="다크모드 토글, JSON 가져오기/내보내기, 전체 데이터 삭제, 앱 정보를 제공합니다."
    />
  );
}
